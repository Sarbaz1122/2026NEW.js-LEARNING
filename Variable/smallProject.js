            //////STUDENT INFORMATION DETAILS//////
            let studentName = "ali";
            let studentFatherName ="ahmed";
            let studentAge = 23;
            let studentClass = "11th";
            let studentRollNo =93939;
            const schoolName = "city school";

            console.log("Name:" + studentName);
            console.log("Father's Name:" + studentFatherName);
            console.log("Age:" + studentAge);
            console.log("Class:" + studentClass);
            console.log("Roll No:" + studentRollNo);
            console.log("School Name:" + schoolName);


            /////Challenge Questions//
            //Q1.Why can var be re-declared but let cannot?
            //ans // ans. Because var allows re-declaration in the same scope,
// while let does not. Also, var is function-scoped and let is block-scoped.

               //Q2. why const must have an initial value?

               //ans.const must have an initial value because its 
               // value is fixed and cannot be reassigned after declaration.



            //Q3. Which keyword should you use most in modern JavaScript: var, let, or const?
            //ans. let and const are use in modern js 

            //Q4. What happens if you write:
//             const age = 20;
// age = 25;
  // ans. It creates an error in the console because const variables
// cannot be reassigned after declaration.

  //Q5. Is myName the same as myname? Why?
    //ans.// ans. No, they are not the same because JavaScript is a case-sensitive language. 
            


